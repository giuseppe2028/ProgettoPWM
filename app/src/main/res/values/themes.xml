<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ProgettoPWM" parent="Theme.AppCompat.Light.NoActionBar.FullScreen">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="textEditStyle">
        <item name="android:padding">@dimen/paddingStartTextEdit</item>
        <item name="background">@drawable/text_edit_shape</item>
    </style>

    <style name="Theme.ProgettoPWM" parent="Base.Theme.ProgettoPWM" />
    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
<style name="bottoneLogin">
    <item name="android:background">@drawable/shape_button</item>
    <item name="android:textColor">@color/white</item>
</style>
    <style name="AccediConGoole">
        <item name="android:backgroundTint">@null</item>
        <item name="android:background">@drawable/shape_button_google</item>
        <item name="android:textColor">@color/white</item>
    </style>
</resources>